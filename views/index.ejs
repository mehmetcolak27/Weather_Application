 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather Forecast</title>
  <style>
    body {
      background-color: #8879df;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      min-height: 100vh;
      font-family: Arial, sans-serif;
    }
    .form {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      margin-top: 2rem;
    }
    label {
      font-size: 1.2rem;
      margin-bottom: 0.5rem;
    }
    input[type="text"] {
      padding: 0.5rem;
      font-size: 1.2rem;
      border-radius: 0.5rem;
      border: 1px solid #ccc;
      margin-bottom: 1rem;
      text-align: center;
    }
    button[type="submit"] {
      padding: 0.5rem 2rem;
      font-size: 1.2rem;
      background-color: #2196f3;
      color: #fff;
      border: none;
      border-radius: 0.5rem;
      cursor: pointer;
    }
    button[type="submit"]:hover {
      background-color: #0d8cf0;
    }
    .forecast {
      margin-top: 2rem;
      text-align: center;
     
    }
    .day {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 2rem;
    }
    .day h2 {
      margin-bottom: 1rem;
    }
    .day .icon {
      width: 10rem;
      height: 10rem;
      margin-bottom: 1rem;
    }
    .day .temp {
      font-size: 2rem;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>Weather Forecast</h1>
  
  <form class="form" action="/" method="POST">
    <label for="cityName">Enter city name:</label>
    <input type="text" id="cityName" name="cityName" placeholder="e.g. London" required autocomplete="off">
    <button type="submit">Get Forecast</button>
  </form>
  <% if (error) { %>
    <p><%= error %></p>
  <% } %>
  <div class="forecast" id="weather-response">
  <% if (typeof weatherData !== 'undefined') { %>
    <h2><%= weatherData.name %></h2>
    <h2>Today</h2>
    <p class="temp"><%= Math.round(weatherData.main.temp) %>°C</p>
    <p>Weather: <%= weatherData.weather[0].description %></p>
    <div class="day">
      <img class="icon" src="https://openweathermap.org/img/wn/<%= weatherData.weather[0].icon %>@2x.png" alt="<%= weatherData.weather[0].description %>">
    </div>
   <% } %>
  </div>
</body>
</html>
